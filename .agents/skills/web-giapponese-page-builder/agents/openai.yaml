interface:
  display_name: "Giapponese Random Builder"
  short_description: "Build a random Japanese item lesson with cards and screenshots"
  default_prompt: "Use $web-giapponese-page-builder to turn a Japanese URL, screenshot, UI prompt, game text or card text into a Giapponese random lesson with cards and screenshot assets."

policy:
  allow_implicit_invocation: true
