import { PlaceholderPage } from "@/components/placeholder-page";
import { routePlaceholders } from "@/lib/site";

export default function SettingsPage() {
  return <PlaceholderPage {...routePlaceholders.settings} />;
}
