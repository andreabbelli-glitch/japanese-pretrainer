import { LoadingShell } from "@/components/ui/loading-shell";

export default function ReviewLoading() {
  return (
    <LoadingShell
      summary="Sto preparando la sessione di review."
      title="Caricamento review"
    />
  );
}
