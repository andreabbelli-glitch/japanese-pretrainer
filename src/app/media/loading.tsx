import { LoadingShell } from "@/components/ui/loading-shell";

export default function MediaLoading() {
  return (
    <LoadingShell
      summary="Sto preparando la libreria dei media."
      title="Caricamento media"
    />
  );
}
