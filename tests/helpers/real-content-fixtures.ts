import path from "node:path";

import { repositoryRoot } from "./content-fixtures";

export const realDuelMastersMediaDirectory = path.join(
  repositoryRoot,
  "content",
  "media",
  "duel-masters-dm25"
);
